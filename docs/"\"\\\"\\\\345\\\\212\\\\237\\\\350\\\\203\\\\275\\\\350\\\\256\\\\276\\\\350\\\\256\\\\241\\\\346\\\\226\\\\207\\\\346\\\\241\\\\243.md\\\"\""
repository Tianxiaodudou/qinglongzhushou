# 青龙助手 - 功能设计文档

> 文档版本：v1.0.0
> 更新日期：2026-05-22
> 状态：设计阶段

---

## 一、项目概述

| 属性 | 说明 |
|------|------|
| 项目名 | 青龙助手 |
| 包名 | `com.qinglong.app` |
| 目标 | 青龙面板 Android 客户端 |
| 技术栈 | Jetpack Compose + Kotlin + Clean Architecture + MVVM + Hilt |

---

## 二、认证机制

### 登录方式
- **用户密码登录**（移动端主用）
  ```
  POST /user/login
  Body: { "username": "...", "password": "..." }
  ```

### 返回值
```json
{
  "code": 200,
  "data": {
    "token": "Bearer eyJhbGciOiJI..."
  },
  "message": "登录成功"
}
```

### 后续请求 Header
```
Authorization: Bearer <token>
```

### Token 存储
- 使用 `EncryptedSharedPreferences` 加密存储
- Key: `token`

---

## 三、服务器配置

| 字段 | 说明 |
|------|------|
| 协议 | HTTP / HTTPS 开关，默认 HTTPS |
| 域名 | 独立输入框，placeholder: `yourdomain.com` |
| 端口 | 独立输入框，数字键盘，placeholder: `5700` |

**Base URL 格式：**
```
{protocol}://{domain}:{port}/
```

---

## 四、功能模块

### 1. 登录
- HTTP / HTTPS 协议切换
- 域名 + 端口分开输入
- 用户名 + 密码登录
- Bearer Token 认证
- 记住登录状态
- 管理已保存的服务器

### 2. 定时任务（默认首页）
- 任务列表（全部 / 运行中 / 已停止）
- 搜索过滤
- 任务状态指示器
- 运行 / 停止任务
- 新建定时任务（FAB）
- 任务详情

### 3. 订阅管理
- 订阅列表
- 搜索过滤
- 新建订阅
- 运行 / 停止订阅
- 编辑 / 删除订阅

### 4. 日志管理
- 日志列表（分页）
- 按任务过滤
- 关键词搜索
- 日志详情查看
- 删除日志

### 5. 环境变量
- 变量列表
- 新建变量
- 编辑 / 删除变量
- 搜索过滤

### 6. 系统状态
- CPU 使用率
- 内存使用率
- 磁盘使用率
- 网络 IO
- 进程列表

### 7. 面板设置
> 对应青龙面板端的设置项

| 功能 | 说明 |
|------|------|
| 基础配置 | 工作目录、代理设置、Git 镜像源 |
| 任务配置 | 并发数、超时时间、重试策略 |
| 通知渠道 | 面板推送、企业微信、钉钉、Telegram 等 |
| 安全设置 | 登录密码修改、IP 白名单、API 令牌管理 |
| 面板信息 | 版本号、在线状态、一键重启 |

### 8. 应用设置
> 管理 APP 本身

| 功能 | 说明 |
|------|------|
| 外观 | 深色/浅色主题、跟随系统 |
| 通知管理 | 任务完成通知、订阅失败通知、日志告警开关 |
| 数据管理 | 清除缓存、导出日志、登录历史 |
| 服务器管理 | 已保存的服务器列表、切换、删除 |
| 关于 | 版本号、用户协议、隐私政策 |
| 退出登录 | 当前服务器登出 |

---

## 五、UI 规范

### TopAppBar 布局（所有页面通用）
```
┌─────────────────────────────────────────────────────┐
│ [☰]  [页面标题]                        [搜索] [⋮] │
└─────────────────────────────────────────────────────┘
```
- **最左**：导航按钮 ☰（固定，所有页面都有）
- **中间**：页面标题
- **最右**：页面级操作按钮

### 导航 Drawer
- 定时任务（默认选中）
- 订阅管理
- 日志管理
- 环境变量
- 系统状态
- 面板设置
- 应用设置
- 底部：用户头像 + 用户名 + 退出登录

### 主题
- Material Design 3
- 深色主题（默认）
- 品牌色：青绿 `#00D9A6`

### 登录界面
- 深色背景 + 绿色 logo
- 表单卡片：圆角 24dp
- HTTP/HTTPS 开关
- 域名/端口独立输入

---

## 六、技术架构

### 项目结构（Clean Architecture）
```
app/src/main/java/com/qinglong/app/
├── data/
│   ├── api/          # Retrofit 接口定义
│   ├── model/        # DTO / Domain Model
│   └── repository/   # 数据仓库
├── di/               # Hilt 依赖注入模块
├── ui/
│   ├── theme/        # Compose 主题（颜色、字体）
│   ├── components/   # 通用 UI 组件
│   ├── navigation/   # 路由定义
│   └── screens/      # 各页面
│       ├── login/
│       ├── task/
│       ├── subscription/
│       ├── log/
│       ├── envvar/
│       ├── system/
│       └── settings/
└── util/            # 工具类
```

### 依赖
| 库 | 版本 |
|---|------|
| Kotlin | 1.9.21 |
| Compose BOM | 2024.01.00 |
| Hilt | 2.48.1 |
| Retrofit | 2.9.0 |
| OkHttp | 4.12.0 |
| Navigation Compose | 2.7.6 |

---

## 七、API 路由

| 方法 | 路由 | 说明 |
|------|------|------|
| POST | /user/login | 登录 |
| POST | /user/logout | 登出 |
| GET | /user/info | 用户信息 |
| GET | /crons | 获取任务列表 |
| POST | /crons | 创建任务 |
| PUT | /crons/{id} | 更新任务 |
| DELETE | /crons/{id} | 删除任务 |
| POST | /crons/{id}/run | 运行任务 |
| POST | /crons/{id}/stop | 停止任务 |
| GET | /subs | 订阅列表 |
| GET | /logs | 日志列表 |
| GET | /envs | 环境变量 |
| GET | /system | 系统状态 |
